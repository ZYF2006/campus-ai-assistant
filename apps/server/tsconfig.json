{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // NodeNext 让 TypeScript 按 Node.js 的 ESM 规则解析模块。
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "types": ["node"],
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src/**/*.ts"]
}
